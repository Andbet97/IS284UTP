/*26) Si la probabilidad de que caiga 6 al lanzar un dado es de 1/6, cual es la probabilidad
de que caiga 6 en todos los n lanzamientos de un dado.*/

//librerias 

#include <stdio.h>
#include <conio.h>

//variables globales, no hay 

//funcion prototipo

double pro1 (int n)
{
	return ((/*ecuacion*/)*100);
}

//funcion principal 

int main (void)
{
	int r;
	printf ("Ingrese cuantos lanzamientos realizo: ");
	scanf ("%d", &r);
	printf ("\nLa probabilidad es de %g por ciento", pro1 (r));
	getch ();
}
