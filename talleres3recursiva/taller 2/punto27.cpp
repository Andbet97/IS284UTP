/*27) Si la probabilidad de que caiga 6 al lanzar un dado es de 1/6, cual es la probabilidad
de que caiga al menos una vez 6 en n lanzamientos del dado.*/

//librerias 

#include <stdio.h>
#include <conio.h>

//variables globales, no hay 

//funcion prototipo

double pro2 (int n)
{
	return ((/*ecuacion*/)*100);
}

//funcion principal 

int main (void)
{
	int r;
	printf ("Ingrese cuantos lanzamientos realizo: ");
	scanf ("%d", &r);
	printf ("\nLa probabilidad es de %g por ciento", pro2 (r));
	getch ();
}
